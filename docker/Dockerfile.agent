FROM python:3.12-slim

WORKDIR /app

COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ .

# Each container runs main.py with its own AGENT_FILTER env (future: per-agent containers)
CMD ["python", "main.py", "--no-ws"]
